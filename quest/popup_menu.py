from quest.game import QuestGame
from quest.map import TiledMap
from quest.dialogue import Dialogue
from quest.modal import Modal, DialogueModal
from quest.sprite import QuestSprite, Player, Wall, NPC
from quest.helpers import scale
from quest.strategy import RandomWalk
import arcade
import os
from pathlib import Path

#NPCs HP are all 3
#NPCs TALK bar is 3
NPC_hp = 3
NPC_talk_hp = 3
def menu():
	if find_NPC{}:
    open(self.menu):
#attack
    	if keyboard.press(‘1’):
			print("The health of NPC{} is lowered")
            hp(NPC{})-1
			return to open(menu)
				If hp(NPC{}) =1:
					print ("NPC{} has ran away")
                return close(self.menu):

		if keyboard.press(‘2’):
			talk(NPC{})-1
			print ("NPC{} says you can find your mom at the end of the supermarket")
			If keyboard.press(“Enter”)
			return to open(menu)
				If talk(NPC{}) =1:
					print ("NPC{} said bye to the user and ran away")
				return close(self.menu):

        if keyboard.press(‘3’):
			print ("user has ran away")
			return close(self.menu):
